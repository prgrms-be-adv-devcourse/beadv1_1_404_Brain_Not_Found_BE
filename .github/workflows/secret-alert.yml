name: 🔔 Secret File Change Alert

on:
#  pull_request:
#    types: [opened, synchronize] # PR이 열리거나 업데이트될 때
#    paths:
#      # Git에 저장된 Secret 파일의 경로를 여기에 지정합니다.
#      # 파일 이름이나 폴더 경로가 변경되면 트리거됩니다.
#      - 'root/k8s-global/prod/services/auth/secret-auth-prod.yaml'
#      - 'root/k8s-global/prod/services/product/secret-product-prod.yaml'
#      - 'root/k8s-global/prod/secret-common-prod.yaml'
#      # 필요하다면 다른 Secret 파일 경로도 추가합니다.

jobs:
#  alert:
#    name: Slack Notification
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: 💬 Send Slack Alert for Secret Update
#        uses: rtCamp/action-slack-notify@v2 # Slack 알림을 위한 인기 액션
#        env:
#          # Webhook URL (Secret에 등록된 이름 사용)
#          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_SECRET_UPDATES }}
#
#          # 알림 메시지 설정
#          SLACK_COLOR: '#FFC300' # 노란색 (경고)
#          SLACK_MESSAGE: |
#            *🚨🚨 Secret 파일 변경 감지 🚨🚨*
#
#            **CI/CD에 사용되는 GitHub Secret 값을 수동으로 업데이트해야 합니다.**
#
#            * PR: <${{ github.event.pull_request.html_url }}|${{ github.event.pull_request.title }}>
#            * 변경 파일: `secret-auth-prod.yaml` (경로 확인)
#            * main 브랜치로 병합 전, GitHub Secrets의 `SECRET_AUTH_PROD` 값을 확인/업데이트하세요.
#
#          SLACK_TITLE: "SECRET MANUAL UPDATE REQUIRED"
#          SLACK_USERNAME: GitHub Actions Alert