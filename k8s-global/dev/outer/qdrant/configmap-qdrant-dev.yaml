apiVersion: v1
kind: ConfigMap
metadata:
  name: qdrant-config
data:
  config.yaml: |
    storage:
      storage_path: "/qdrant/storage"
    service:
      grpc_port: 6334
      http_port: 6333